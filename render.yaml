services:
  - type: web
    name: fxpro-backend
    env: node
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true

  - type: web
    name: fxpro-frontend
    env: static
    region: oregon
    plan: free
    rootDir: frontend
    autoDeploy: true
